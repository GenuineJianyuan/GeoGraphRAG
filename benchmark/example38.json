{
    "query": "Analyze logistics sites spatial distribution",
    "datasource": [
        "User-generated point vector data",
        "Spatial analysis raster data"
    ],
    "raw_code": "var points = ee.List.sequence(latMin,latMax,(latMax-latMin)/numPoints)\n  .zip(ee.List.sequence(lonMin,lonMax,(lonMax-lonMin)/numPoints))\n  .map(function(coords){\n    return ee.Feature(ee.Geometry.Point(ee.List(coords).get(1), ee.List(coords).get(0)));\n  });\n\nvar densityMap = ee.Image.pixelArea().divide(ee.Image.constant(numPoints))\n  .reduceRegions({\n    collection: pointsCollection,\n    reducer: ee.Reducer.count(),\n    scale:1000\n  });\n\nvar clusterer = ee.Clusterer.wekaKMeans(5).train(coordinatesList);\nvar clustered = pointsCollection.cluster(clusterer);",
    "api": [
        "ee.List.sequence",
        "ee.Geometry.Point",
        "ee.Image.pixelArea",
        "ee.Image.constant",
        "ee.Reducer.count",
        "ee.Clusterer.wekaKMeans",
        "ee.Clusterer.train"
    ],
    "processing step": [
        "Generate list of lat/lon points within given bounds",
        "Create feature collection from generated points",
        "Calculate density map using pixel area and point count",
        "Reduce regions to count points within specified scale",
        "Train KMeans clusterer with specified number of clusters",
        "Cluster points using trained KMeans model"
    ],
    "output": "**Map of clustered point density.**"
}